import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "../components/Header";
import { WalletConnectProvider } from "@/lib/walletconnect-context";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ceypto - Cryptocurrency Trading & Portfolio Management",
  description: "Track, trade, and manage your cryptocurrency portfolio with real-time data and advanced tools",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <WalletConnectProvider>
          <Header />
          {children}
        </WalletConnectProvider>
      </body>
    </html>
  );
}
